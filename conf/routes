# Routes
# This file defines all application jdbc_app.anormapp.anormapp.slickapp.routes (Higher priority jdbc_app.anormapp.anormapp.slickapp.routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# sub projectsへルーティング
# slickの場合jdbcを使えないため、slickプロジェクトを有効化するときはここをコメントアウト
# ->      /jdbc                       jdbcapp.Routes
# ->      /anorm                      anormapp.Routes
->      /slick                      slickapp.Routes

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
